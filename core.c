#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include "terminal.h"
#include "gui.h"
#include "core.h"
#include "config.h"

static char* mapFile; 
static char* map;

/* Prompts user for map file and initializes map array. */
void initMap () {
    char ch;
    do {

    } while (1);
}